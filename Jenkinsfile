pipeline { 
  
   agent any

   stages {
     stage('Test') { 
        steps { 
           sh 'echo "testing application..."'
        }
      }

         stage("Deploy application") { 
         steps { 
           sh 'echo "deploying application..."'
         }

     }
      stage("test") { 
         steps { 
           sh 'echo "for testing"'
         }
      }
   	}

   }
